import type { NextSeoProps } from 'next-seo';

const defaultSeo: NextSeoProps = {
  openGraph: {
    type: 'website',
    locale: 'ko_KR',
    site_name: 'mandala plan',
    description: '2022 mandala plan',
  }
};

export default defaultSeo;
